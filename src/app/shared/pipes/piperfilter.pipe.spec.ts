import { PiperfilterPipe } from './piperfilter.pipe';

describe('PiperfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PiperfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
